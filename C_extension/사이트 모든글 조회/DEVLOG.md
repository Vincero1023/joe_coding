# Devlog

## 짧게 쓰는 규칙 (3줄)
1. 한 항목당 3~5줄 안에서 핵심만 적는다.
2. 증상-원인-해결-검증 순서로만 기록한다.
3. 추측은 "가정"으로 표시하고, 확인 후 즉시 갱신한다.

## Date
- 2026-03-11 01:42 (KST)

## What changed (변경점)
- `AI_CONTEXT.md`, `TASK.md`, `AI_RULES.md`에 프로젝트 목적과 제약을 워드프레스 URL 수집용 Chrome Extension 기준으로 구체화했다.

## Why (원인/배경)
- 현재 저장소에는 구현 코드가 없고 템플릿 문서만 있어, 먼저 범위와 금지사항을 고정하지 않으면 이후 구현 방향이 흔들릴 수 있었다.

## How verified (검증 방법/체크리스트)
- [x] 문서 파일 3개 수정 완료
- [x] 워드프레스 공개 URL 수집, 최소 권한, 탐지 회피 제외 원칙 반영 확인
- [ ] 실제 익스텐션 실행 검증은 아직 없음
- [x] 회귀 영향 없음(코드 파일 없음)

## Issues & Fix (문제-원인-해결)
- 문제: 프로젝트 목표와 안전 경계가 비어 있었다.
- 원인: 초기 템플릿만 있고 실제 요구사항이 문서화되지 않았다.
- 해결: 공개 수집 범위, 금지 기능, 다음 작업 우선순위를 문서에 명시했다.

## Next (다음 작업)
- 공개 수집 전략 문서화
- Manifest V3 초기 구조 생성

## Date
- 2026-03-11 02:05 (KST)

## What changed (변경점)
- `manifest.json`, `popup.html`, `popup.css`, `popup.js`, `collector.js`를 추가해 MV3 익스텐션 골격과 공개 URL 수집 로직을 구현했다.
- `COLLECTION_STRATEGY.md`를 추가해 수집 순서와 제한 조건을 문서화했다.

## Why (원인/배경)
- 사이트맵에 없는 공개 글 URL까지 최대한 수집하려면 단일 엔드포인트가 아니라 여러 공개 소스를 순차적으로 조회해야 했다.

## How verified (검증 방법/체크리스트)
- [x] `node --check`로 `collector.js`, `popup.js` 문법 확인
- [x] `manifest.json` 구조 확인
- [ ] Chrome에서 압축해제된 확장 프로그램 로드 테스트는 아직 안 함
- [x] 회귀 영향 없음(기존 실행 코드 없음)

## Issues & Fix (문제-원인-해결)
- 문제: 파이프 기반 문법 검사에서 한글 문자열 인코딩으로 오탐이 발생했다.
- 원인: PowerShell 파이프 경유 시 입력 인코딩이 깨져 Node가 문자열 경계를 잘못 해석했다.
- 해결: 임시 `.mjs` 파일로 복사해 `node --check`를 직접 실행하는 방식으로 검증했다.

## Next (다음 작업)
- Chrome 수동 로드 및 실제 사이트 테스트
- 결과 저장 형식(TXT 외 JSON/CSV) 확장 여부 결정

## Date
- 2026-03-11 02:34 (KST)

## What changed (변경점)
- `analyzer.js`를 추가해 제목 키워드, 제목 패턴, 카테고리, 인기 추정, 수익화 신호 분석을 구현했다.
- `collector.js`를 확장해 URL별 공개 노출 소스와 아카이브 기반 인기 신호를 함께 반환하도록 바꿨다.
- `popup.html`, `popup.css`, `popup.js`, `manifest.json`을 벤치마킹 분석기 UI와 TSV/JSON 내보내기 기준으로 갱신했다.

## Why (원인/배경)
- 단순 URL 목록만으로는 블로그 벤치마킹 가치가 낮아서, 공개적으로 보이는 제목/구조/수익화 신호를 한 번에 읽어야 했다.

## How verified (검증 방법/체크리스트)
- [x] `node --check`로 `collector.js`, `analyzer.js`, `popup.js` 문법 확인
- [x] `popup.html` 구조 확인
- [ ] Chrome에서 압축해제된 확장 프로그램 로드 테스트는 아직 안 함
- [x] 회귀 영향 없음(기존 실행 코드 없음)

## Issues & Fix (문제-원인-해결)
- 문제: 큰 패치를 한 번에 적용하려다 Windows 명령 길이 제한에 걸렸다.
- 원인: 다수 파일 교체와 신규 파일 추가를 한 번의 `apply_patch`로 처리했다.
- 해결: 수집기, 분석기, 팝업 파일을 나눠 순차 적용했다.

## Next (다음 작업)
- 실제 워드프레스 사이트 2~3곳 수동 검증
- 다중 사이트 비교 리포트 여부 결정

## Date
- 2026-03-12 00:20 (KST)

## What changed (변경점)
- `analyzer.js`에 수익형 키워드 점수, 머니페이지 점수/유형 판정, 제목 공식 라이브러리, 기회 점수 계산을 추가했다.
- `popup.html`, `popup.css`, `popup.js`를 4개 탭(`키워드`, `머니페이지`, `제목 공식`, `기회 점수`) 중심의 단순 UI로 재구성했다.
- 내보내기 TSV에 `Commercial`, `MoneyPage`, `Type` 컬럼을 추가했다.

## Why (원인/배경)
- 기존 화면은 정보가 많았지만 바로 수익형 글 기획으로 연결되기 어려워, 수익화 판단에 필요한 결과만 먼저 보이도록 줄일 필요가 있었다.

## How verified (검증 방법/체크리스트)
- [x] `node --check`로 `analyzer.js`, `popup.js`, `collector.js` 문법 확인
- [x] 탭형 `popup.html` 구조 확인
- [ ] Chrome에서 압축해제된 확장 프로그램 로드 테스트는 아직 안 함
- [x] 회귀 영향 없음(기존 코드는 정적 분석 통과)

## Issues & Fix (문제-원인-해결)
- 문제: `머니페이지 수`가 전체 후보가 아니라 상위 목록 길이만 집계되고 있었다.
- 원인: 요약 수치 계산에 `top N` 리스트 길이를 그대로 사용했다.
- 해결: 전체 분석 글 중 `isMoneyPage`가 참인 개수를 별도로 계산해 요약 수치에 반영했다.

## Next (다음 작업)
- 실제 워드프레스 사이트에서 탭별 결과 타당성 검증
- 다중 사이트 비교 리포트 여부 결정

## Date
- 2026-03-16 00:00 (KST)

## What changed (변경점)
- `TASK.md`, `AI_CONTEXT.md`를 현재 상태 기준으로 정리해 다음 세션 시작점을 `실사이트 검증`으로 고정했다.

## Why (원인/배경)
- 구현은 한 차례 완료됐고, 다음 단계는 기능 추가보다 점수 모델과 실제 결과의 일치 여부를 검증하는 것이 우선이다.

## How verified (검증 방법/체크리스트)
- [x] 현재 Todo/결정사항/다음 프롬프트를 문서에 반영
- [x] 프로젝트 목표와 출력 설명을 최신 UI 기준으로 갱신
- [ ] Chrome 수동 테스트는 아직 안 함
- [x] 코드 수정 없음

## Issues & Fix (문제-원인-해결)
- 문제: 다음 세션에서 무엇부터 확인해야 하는지 문서상 우선순위가 약했다.
- 원인: 구현 기록은 있었지만 `이제는 검증 단계`라는 상태 전환이 분명히 적혀 있지 않았다.
- 해결: 다음 세션의 첫 작업을 `실사이트 검증`으로 명시하고 다음 프롬프트도 그 기준으로 고정했다.

## Next (다음 작업)
- 실제 워드프레스 사이트 2~3곳 탭별 결과 검증
- 점수 모델 보정 포인트 기록

## Date
- YYYY-MM-DD HH:mm (KST)

## What changed (변경점)
- [수정한 파일/기능]
- 예시: `update_3gyogu_hwpx.py`의 이미지 매핑 분기 로직 수정

## Why (원인/배경)
- [왜 바꿨는지]
- 예시: 특정 행에서 floating image id가 누락되어 사진이 교체되지 않음

## How verified (검증 방법/체크리스트)
- [ ] 로컬 재실행 완료
- [ ] 출력 파일 생성 확인
- [ ] 핵심 케이스 N건 수동 확인
- [ ] 회귀(기존 기능) 이상 없음

## Issues & Fix (문제-원인-해결)
- 문제: [...]
- 원인: [...]
- 해결: [...]

## Next (다음 작업)
- [다음 1~2개 작업]
